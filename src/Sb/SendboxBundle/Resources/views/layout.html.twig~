{% set _side  = block('side') %}
{% set _content  = block('content') %}
{% set _javascript  = block('javascript') %}
<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="utf-8">
        <title>{% block title %}Goodcoin{% endblock %}</title>
        {% block javascripts %}
          {% javascripts
              '@sbSendboxBundle/Resources/public/js/jquery-1.7.1.min.js'
              '@sbSendboxBundle/Resources/public/js/jquery-ui-1.8.16.custom.min.js'
              '@sbSendboxBundle/Resources/public/js/main.js' %}
            <script type="text/javascript" src="{{ asset_url }}"></script>
          {% endjavascripts %}
        {% endblock %}
        {% block stylesheets %}
          {% stylesheets '@sbSendboxBundle/Resources/public/css/main.css' %}
            <link rel="stylesheet" type="text/css" media="screen" href="{{ asset_url }}" />
          {% endstylesheets %}
        {% endblock %}
        <link rel="shortcut icon" href="{{ asset('/bundles/spspotora/favicon.ico') }}" />
    </head>
    <body>
    {% if _javascript is not empty %}
      <script>
        $(function() {
          {{ _javascript|raw }}
        });
      </script>
    {% endif %}
    {% block body %}
      <header id="header">
        {% include 'SbSendboxBundle::header.html.twig'%}
      </header>
      {% block messages %}{% endblock %}
      <div id="content" class="container">
        {% if _content is not empty %}
          <div id="container" class="{% if _side is empty %}oneColumn{% endif %}" >{{ _content|raw }}</div>
        {% endif %}
        {% if _side is not empty %}
          <div id="sidebar" >{{ _side|raw }}</div>
        {% endif %}
      </div>
      <footer id="footer">
        {% include 'SbSendboxBundle::footer.html.twig'%}
      </footer>  
     {% endblock %}
    </body>
</html>
